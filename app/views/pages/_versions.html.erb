<h2>Previous Versions</h2>

<table class='table'>
  <thead>
    <tr>
      <th>Index</th>
      <th>Date</th>
      <th>Author</th>
      <th>Event</th>
      <th>Word Count</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%- page.versions.reverse.each do |version| %>
      <tr>
        <td><%= version.index %></td>
        <td><%= version.created_at %></td>
        <td><%= find_version_author_name(version) %></td>
        <td><%= version.event.humanize %></td>
        <td><%= version.word_count %></td>
        <td><%= link_to 'Diff', diff_page_version_path(page, version) %></td>
        <td><%= link_to 'Rollback', rollback_page_version_path(page, version), method: 'PATCH' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
