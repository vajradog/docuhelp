<i class="fa fa-book"></i>
<% if activity.trackable %><%= activity.owner.username if activity.owner %> created a new page:
 <strong><%= link_to activity.trackable.title, activity.trackable %></strong>
<% else %>
A file was deleted by <%= activity.owner.username if activity.owner %>.<%= link_to ' Click here to learn more or recover the file', deleted_pages_path %>
<% end %>
